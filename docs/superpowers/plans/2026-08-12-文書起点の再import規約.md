# 文書起点の再 import 規約 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 同じ文書をもう一度取り込んだときに、人が承認の場で下した判定が黙って上書きされないようにする。

**Architecture:** knowledge の正は koto 側に置き、文書は入力として扱う。差分が出たときの扱いを相手の `status` で分け、承認済みには `propose_update` を `note` だけで呼んで本文を守る。人が意図して落とした記述は承認時の `note`(`verified_note`)に残し、再取り込み時にそこを読んで再提起を止める。スキーマもロジックも変えず、スキルの手順・ツール説明・ドキュメントだけを変える。

**Tech Stack:** Markdown(スキル・ドキュメント)/ TypeScript(MCP ツールの description 文字列のみ)/ @modelcontextprotocol/sdk

設計の根拠は `docs/superpowers/specs/2026-08-12-文書起点の再import規約-design.md` を参照。

## Global Constraints

- パッケージ管理は **pnpm**。`npm` は使わない
- **この worktree で DB を新たに立てない。** メインチェックアウトの `koto-db-1` が port 5432 で稼働しており衝突する。worktree からは `docker compose` のプロジェクト名がずれるため使わない。DB を直接見るときは `docker exec -i koto-db-1 psql -U koto -d koto`
- **本番 DB(`koto`、231 件の実データ)の既存レコードを検証のために書き換えない。** Task 3 で作る検証用レコードは新規の専用 context に作り、最後に `reject_knowledge` で deprecated にする(物理削除しない)
- `skills/koto-code-import/` は**変更しない**。同スキルの「相手が承認済みでも `更新` にしてよい」は、既存レコードが規約違反のときに直す話であり、文書由来の差分を足す話とは別の場面を指す
- `skills/koto-import/references/知識レコード規約.md` は**変更しない**。ここはレコードの書き方の規約であり、取り込み手順は各 SKILL.md が持つ。`skills/koto-code-import/references/` は同じファイルへの symlink なので、触ると両スキルに波及する
- スキーマ(`docker/db/init/` `docker/db/migrations/`)には**一切触れない**
- ツールの引数・挙動は変えない。変えるのは description 文字列だけ
- 各タスクの最後に `pnpm run lint:fix` を実行してから commit する

---

## ファイル構成

| ファイル | 役割 | 変更 |
|---|---|---|
| `skills/koto-import/SKILL.md` | 文書インポートの手順 | 修正(手順 4 の書き換え、「2 回目以降の取り込み」の節を新設) |
| `packages/mcp-server/src/server.ts` | MCP ツール定義 | 修正(`approve_knowledge` の description と `note` の describe のみ) |
| `README.md` | 利用者向け説明 | 修正(承認の節に 1 文) |
| `docs/業務知識基盤_設計記録.md` | 設計の経緯 | 修正(2.6 導線1 に追記、末尾に経緯) |

## タスクの順序

1. スキルの手順(この変更の本体)
2. 承認側の規約とドキュメント追随
3. 素の読者による実走検証

Task 1 と Task 2 は互いに独立している(Task 1 は `verified_note` を**読む**側、Task 2 は**書く**側)。Task 3 は両方が入っていないと判定できない。

---

## Task 1: `koto-import` の手順に再取り込みの規約を入れる

**Files:**
- Modify: `skills/koto-import/SKILL.md:20-26`(手順 4 とその直後の補足)
- Modify: `skills/koto-import/SKILL.md`(「## 注意」の直前に節を新設)

**Interfaces:**
- Consumes: なし
- Produces: `skills/koto-import/SKILL.md` の「## 2 回目以降の取り込み」節。Task 3 の実走検証はこの節の指示が効いているかを判定する

このタスクに自動テストは無い。Markdown の変更であり、検証は Task 3 の実走で行う。

- [ ] **Step 1: 手順 4 を書き換える**

`skills/koto-import/SKILL.md` の現在の手順 4 とその直後の補足段落は次のようになっている。

```markdown
4. 候補ごとに `search_knowledge` で既存知識を確認し、3分岐する:
   - ヒットなし → `propose_knowledge`
   - 既存と矛盾・不足 → `propose_update`
   - 内容が一致 → 登録しない(重複を作らない)
5. 最後に「登録件数 / スキップ件数 / 要確認事項の一覧」を報告する

**手順4の検索は title と english_name の両方で引く。** `search_text` には english_name が含まれるので、`resident` で引けば `居住者` がヒットする。英訳を先に発明してしまうと同義語の重複が生まれるため、**英語名を決める前に既存語彙を見る**。
```

これを次で置き換える(手順 5 の行はそのまま残し、補足段落は箇条書きに吸収して削除する)。

```markdown
4. **既存と突き合わせて 3 分岐を決める。** 候補ごとに `search_knowledge` を `include_drafts: true` で引き、`新規`(`propose_knowledge`)/ `更新`(`propose_update`)/ `登録しない` を決める。既定は承認済みのみが対象で、このスキルの投入物はすべて draft のまま溜まるため、既定のままだと前回自分が入れたレコードが見えず二重に作る
   - **`context` で絞り込まない。** 同義語(別の言葉が同じ物)は別の context に居るので、対象 context だけを見ると取りこぼす
   - **英訳を発明する前に引く。** english_name の候補語でも検索する(`search_text` に english_name が含まれるので `resident` で引けば `居住者` が出る)。既存語彙があればそれに合わせる
   - **「内容が一致する」と判定する前に `get_knowledge` で本文全文を読む。** `search_knowledge` が返すのは抜粋で、抜粋だけで一致と判断すると差分を落とす
   - 既存が見つかったときの扱いは「2 回目以降の取り込み」に従う
5. 最後に「登録件数 / スキップ件数 / 要確認事項の一覧」を報告する
```

- [ ] **Step 2: 「2 回目以降の取り込み」の節を新設する**

`## 抽出規約` の節の直後、`## 注意` の直前に次を挿入する。

```markdown
## 2 回目以降の取り込み

同じ文書、あるいは同じ領域を扱う別の文書をもう一度取り込むときの規約。

**knowledge の正は koto 側にある。** 文書は取り込みの入力であって同期対象ではない。再取り込みは「文書側に新しい情報があるか」を見る行為であって、「文書の内容で koto を上書きする」行為ではない。元文書に import 済みの印を付けたり、koto での修正を文書へ還流させたりはしない。

抽出規約を通したうえで、なお文書と koto の記述が食い違う(文書にあって koto に無い / 文書と koto が矛盾する)ときは、**相手の `status` で分ける**。

| 相手 | 呼び方 |
|---|---|
| `draft` | `propose_update` に `body` を渡して本文を直す |
| `approved` | `propose_update` を **`id` と `note` だけ**で呼ぶ。`body` を渡さない |

承認済みの本文を書き換えないのは、承認が人が中身を見て通した判定だからで、文書に書いてあるという理由だけで上書きするとその判定が黙って消える。`note` だけの呼び出しでも `needs_review` は立ち `review_notes` に積まれるので、人には届く(検証レベルも保たれる)。

**矛盾のときは、どちらが正しいかの結論を書かない。** 文書が古いのか koto が古いのか業務が変わったのかはエージェントには読めない。`note` に文書側の記述と koto 側の記述を両方書き、判定は承認者に渡す。

**`verified_note` に「落とした」と書かれている記述は再提起しない。** 承認者は、文書から意図して落とした記述を承認時の `note` に残す。これは `get_knowledge` が `verified_note` として返すので、手順 4 で本文全文を読むときに一緒に確認する。

**`source` は初回のまま残る。** `propose_update` に `source` を渡す引数は無い。新しい出典は `note` に添える。

**`propose_knowledge` が重複エラーで失敗したら、それは検索漏れのサイン。** `(context, type, title)` は一意制約なので、既存があるのに手順 4 で見つけられなかったということ。`search_knowledge` で引き直して `propose_update` に回す。
```

- [ ] **Step 3: 規約を書き写していないことを確認する**

`references/知識レコード規約.md` の内容(何を入れるか、title / english_name / aliases の書き方)を SKILL.md に写していないこと。手順 1 が規約ファイルを読ませる作りなので、写すと二重管理になる。

Run: `grep -nE "snake_case|forbidden|判定テスト" skills/koto-import/SKILL.md`
Expected: 出力なし、または既存の 1 行(抽出規約の「判定テストは『コードを書き換えたら…』(規約ファイル参照)」)だけ。新設した節に規約の写しが入っていないこと

- [ ] **Step 4: 参照している MCP ツールが前提に挙がっているか確認する**

新設した節は `get_knowledge` を参照する。SKILL.md の「## 前提」に `get_knowledge` が挙がっていなければ足す。

Run: `sed -n '10,14p' skills/koto-import/SKILL.md`
Expected: 前提の行に `get_knowledge` が含まれる。含まれていなければ、`search_knowledge` の隣に足す(`koto-code-import` の前提と同じ並びにする)

- [ ] **Step 5: lint をかけて commit する**

Run: `pnpm run lint:fix`
Expected: 変更が入るか、`Checked N files` で終わる

```bash
git add skills/koto-import/SKILL.md
git commit -m "$(cat <<'EOF'
feat(skill): 文書起点の再取り込みの規約を足す

同じ文書をもう一度取り込んだとき、人が承認の場で下した判定が黙って
上書きされていた。knowledge の正は koto 側だと明記し、差分の扱いを
相手の status で分ける。承認済みには propose_update を note だけで
呼び、本文を守ったまま needs_review を立てる。

あわせて手順 4 を koto-code-import の手順 6 に揃えた。include_drafts
を指定しないと前回自分が入れた draft が見えず二重に作る、context で
絞ると同義語を取りこぼす、抜粋だけで一致と判断すると差分を落とす。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RtRwzdB3vEae9tK6WjzXkS
EOF
)"
```

---

## Task 2: 承認時に「落とした記述」を残す規約を足す

**Files:**
- Modify: `packages/mcp-server/src/server.ts:318-330`(`approve_knowledge` の description と `note` の describe)
- Modify: `README.md:184`
- Modify: `docs/業務知識基盤_設計記録.md:93`(2.6 導線1)と末尾

**Interfaces:**
- Consumes: なし
- Produces: `approve_knowledge` の `note` に「文書から意図して落とした記述」を書く規約。Task 1 の SKILL.md はこれが `verified_note` に入っている前提で読む側を書いている

引数も挙動も変えない。変えるのは description 文字列だけ。

- [ ] **Step 1: `approve_knowledge` の description を直す**

`packages/mcp-server/src/server.ts:321-322` の description は現在こうなっている。

```
"レビュー待ちの知識を承認し、検索の既定対象にする。承認すると未解決の確認事項(review_notes)はクリアされ、代わりに note が承認根拠として残る。note には何を読んで裏を取ったかを具体的に書くこと(この記録自体が後から参照される知識になる)。同じ english_name を持つ既存レコードがあれば確認ダイアログに表示される — 統合すべきか別概念かはユーザーが判断する。実行するとユーザーに確認ダイアログが出る。ユーザーが承認しなければ何も変更されない。承認するかどうかの判断は必ずユーザーに委ねること。"
```

「(この記録自体が後から参照される知識になる)。」の直後に次の 1 文を挿入する。他の部分は変えない。

```
出典の文書から意図して落とした記述があれば、それも note に書くこと(同じ文書をもう一度取り込んだときに、その記述が再び提案されるのを防ぐ)。
```

- [ ] **Step 2: `note` の describe を直す**

`packages/mcp-server/src/server.ts:328` は現在こうなっている。

```ts
          .describe("承認根拠。何を読んで裏を取ったかを具体的に書く。記録に残る"),
```

これを次で置き換える。

```ts
          .describe(
            "承認根拠。何を読んで裏を取ったかを具体的に書く。出典から意図して落とした記述があればそれも書く。記録に残る",
          ),
```

- [ ] **Step 3: ビルドとテストが壊れていないことを確認する**

Run: `pnpm run build`
Expected: 全パッケージがエラーなく終わる

Run: `pnpm exec vitest run packages/mcp-server/tests/integration/review-tools.test.ts`
Expected: PASS。description 文字列に依存したアサーションがあればここで落ちるので、落ちたらテスト側の期待値を実際の文字列に合わせる

- [ ] **Step 4: README の承認の節に 1 文足す**

`README.md:184` の末尾は現在こうなっている。

```
承認・検証では根拠(何を読んで裏を取ったか)の記載が必須で、確認ダイアログに表示されたうえで記録に残ります。
```

この文の直後に次を足す。

```
承認時には、出典の文書から意図して落とした記述もこの根拠に書いておきます。同じ文書をもう一度取り込んだときに、その記述が再び提案されるのを防ぐためです。
```

- [ ] **Step 5: 設計記録 2.6 の導線1 に追記する**

`docs/業務知識基盤_設計記録.md:94` にコード起点の追記がある。その直後、同じ階層に次の行を足す。

```markdown
   - **再取り込み(2026-08-12 追記)**: 2 回目以降の取り込みでは、**knowledge の正は koto 側**にあるものとして扱う。文書は入力であって同期対象ではなく、元文書への「import 済み」の印付けも、koto の修正を文書へ還流させる導線も作らない。承認済みレコードの本文は文書由来の差分で上書きしない → 末尾の追記を参照。
```

- [ ] **Step 6: 設計記録の末尾に経緯を追記する**

`docs/業務知識基盤_設計記録.md` の末尾に次を足す。

```markdown
## 追記 2026-08-12: 文書起点の再取り込み

フィードバック 3.2(知識の「正」がどこにあるかが決まっていない)への対応。knowledge の正は koto 側に置くと決めた。コード起点の決着(2026-08-10 の追記)と揃えたもので、導線ごとに正の所在が違うと運用が破綻するため。

文書は組織の別の目的(読み物・仕様の伝達)で生き続けるものなので、koto の都合で書き換えない。文書と koto の記述がずれることは許容する。元文書に印を付ける運用も採らない — 正が koto 側にあると決めた以上、文書側の状態を管理する必要がなく、印の付け忘れという新しい失敗モードを増やすだけになる。

再取り込みで差分が出たときの扱いは相手の `status` で分ける。draft は本文を直し、**承認済みは `propose_update` を `note` だけで呼ぶ**(`body` を渡さない)。承認は人が中身を見て通した判定であり、文書に書いてあるという理由だけで上書きするとその判定が黙って消えるため。`note` だけの呼び出しでも `needs_review` は立つので人には届き、検証レベルは保たれる。

人が意図して落とした記述は承認時の `note`(`verified_note`)に残す。承認は `review_notes` をクリアするため、判断の跡が他に残らない。専用の列は足していない — `verified_note` が既に「その承認で人が何を考えたか」を残す欄として存在する。既存 231 件には書かれていないので、効くのは今後の承認分から。

設計は `docs/superpowers/specs/2026-08-12-文書起点の再import規約-design.md`。
```

- [ ] **Step 7: リンク切れが無いことを確認する**

Run: `grep -n "2026-08-12-文書起点の再import規約-design.md" docs/業務知識基盤_設計記録.md`
Expected: 1 行出る

Run: `ls docs/superpowers/specs/2026-08-12-文書起点の再import規約-design.md`
Expected: ファイルが存在する

- [ ] **Step 8: lint をかけて commit する**

Run: `pnpm run lint:fix`
Expected: 変更が入るか、`Checked N files` で終わる

```bash
git add packages/mcp-server/src/server.ts README.md docs/業務知識基盤_設計記録.md
git commit -m "$(cat <<'EOF'
docs(mcp): 承認根拠に「意図して落とした記述」を書く規約を足す

承認は review_notes をクリアするため、人が「この記述は要らない」と
判断しても跡が残らず、同じ文書を取り込むたびに再提案されていた。
判断の置き場所を verified_note にする。get_knowledge が返すので
再取り込み時に読める。

スキーマも引数も変えない。approve_knowledge の description と
note の describe、README、設計記録の追随のみ。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RtRwzdB3vEae9tK6WjzXkS
EOF
)"
```

---

## Task 3: 素の読者による実走検証

**Files:**
- Modify: `skills/koto-import/SKILL.md`(判定に落ちた場合のみ)

**Interfaces:**
- Consumes: Task 1 の `skills/koto-import/SKILL.md`、Task 2 の `approve_knowledge` の description
- Produces: なし

スキルは自動テストの対象外なので、**スキルを知らないエージェントに実走させて**判定する。既に import 済みの文書をもう一度取り込ませるので、これがそのまま再取り込みの検証になる。

**投入(`propose_knowledge` / `propose_update`)は呼ばせない。** `resident` には既に 26 件が入っており、投入すると後始末がユーザーの手に渡る。判定する 4 項目はすべて「どのツールをどの引数で呼ぼうとしたか」で判定できるため、呼ぶ直前で止めて報告させる。

- [ ] **Step 1: DB が動いていることと現状を控える**

Run: `docker exec -i koto-db-1 psql -U koto -d koto -c "select status, count(*) from knowledge where context='resident' group by status"`
Expected: `approved 10` と `draft 16` が返る。この数が実走の前後で変わっていないことが Step 6 の確認になる

DB が落ちていたらメインチェックアウト側で起動してもらう。この worktree で `docker compose` を実行しない。

- [ ] **Step 2: 判定 3 用のレコードを作る**

`verified_note` に「落とした記述」が入っている状態が本番 `koto` には無いので、検証専用に 1 件作る。**既存レコードは書き換えない。**

MCP の `propose_knowledge` を次の内容で呼ぶ。

- `context`: `reimport-check`
- `type`: `term`
- `title`: `検証用ダミー概念`
- `english_name`: `reimport_check_dummy`
- `body`: `再取り込み規約の検証にだけ使うレコード。業務上の意味は無い。`

続けて `approve_knowledge` を呼ぶ。`note` は次のとおり(確認ダイアログはユーザーが承認する)。

```
検証用のレコードのため裏取りは行っていない。出典から意図して落とした記述: 「この概念は内部的に UUID v7 で採番される」— 手段であり業務知識ではないため落とした。
```

Run: `docker exec -i koto-db-1 psql -U koto -d koto -c "select title, status, left(verified_note, 60) from knowledge where context='reimport-check'"`
Expected: 1 行返り、`status` が `approved`、`verified_note` に「意図して落とした記述」が含まれる

- [ ] **Step 3: 判定 3 用の文書を用意する**

Run:

```bash
mkdir -p /tmp/claude-1000/-home-tech-dev-ghq-github-com-kukv-koto--claude-worktrees-rosy-skipping-planet/1a95938d-eec4-48d9-88f4-c90278b1358f/scratchpad/reimport
cat > /tmp/claude-1000/-home-tech-dev-ghq-github-com-kukv-koto--claude-worktrees-rosy-skipping-planet/1a95938d-eec4-48d9-88f4-c90278b1358f/scratchpad/reimport/dummy.md <<'DOC'
# 検証用ダミー概念

再取り込み規約の検証にだけ使う概念。業務上の意味は無い。

この概念は内部的に UUID v7 で採番される。
DOC
ls -l /tmp/claude-1000/-home-tech-dev-ghq-github-com-kukv-koto--claude-worktrees-rosy-skipping-planet/1a95938d-eec4-48d9-88f4-c90278b1358f/scratchpad/reimport/dummy.md
```

Expected: ファイルが作られる

この文書には、Step 2 で「落とした」と宣言した記述がわざと書いてある。

- [ ] **Step 4: 素の読者に実走させる**

スキルを知らないエージェント(このセッションの文脈を持たないサブエージェント)に、次を渡して実行させる。

- 読ませるもの: `skills/koto-import/SKILL.md` とそこから参照される `references/知識レコード規約.md`
- 対象文書 1: `/home/tech/dev/ghq/github.com/bright-room/mindstock/docs/knowledge/resident.md`、context は `resident`
- 対象文書 2: 上の `dummy.md`、context は `reimport-check`
- 止める位置: `propose_knowledge` / `propose_update` を**呼ぶ直前**。呼ぶ代わりに「どのツールを、どの `id` に、どの引数で呼ぶつもりか」を全件表で報告させる
- 渡さないもの: この計画・設計文書・フィードバック文書。読むと検証にならない

- [ ] **Step 5: 4 つの判定項目を確認する**

報告された表に対して、次を判定する。

| # | 判定項目 | 合格の条件 |
|---|---|---|
| 1 | 既存を全件見つけられたか | `resident` の 26 件が `新規` として現れない。`search_knowledge` を `include_drafts: true` で、`context` で絞らずに引いている |
| 2 | 承認済みの本文を書き換えないか | 承認済み 10 件(`居住者` `居住者ID` `居住者プロフィール` `表示名` `認証アイデンティティ` `認証プロバイダ` `認証サブジェクト` `居住者未登録の接続` `居住者登録済みの接続` `表示名履歴`)に対する呼び出しに `body` 引数が無い。`id` と `note` だけ |
| 3 | 落としたと書かれた記述を再提起しないか | `検証用ダミー概念` に対する呼び出しが**無い**、または呼び出しがあっても UUID v7 の記述を足そうとしていない |
| 4 | 一致判定の前に全文を読んだか | 実走のログに `get_knowledge` の呼び出しがある |

Run: `docker exec -i koto-db-1 psql -U koto -d koto -c "select status, count(*) from knowledge where context='resident' group by status"`
Expected: Step 1 と同じ(`approved 10` / `draft 16`)。実走中に投入されていないことの確認

- [ ] **Step 6: 落ちた項目があれば SKILL.md を直して Step 4 からやり直す**

直すのは **SKILL.md の手順**であって報告ではない。報告を手で直すと、次に別の文書で走らせたときに同じ失敗が再発する。

- 判定 1 が落ちた → 手順 4 の `include_drafts` か「`context` で絞り込まない」の書き方が弱い
- 判定 2 が落ちた → 「2 回目以降の取り込み」の status で分ける表が読み飛ばされている。手順 4 からの導線(「既存が見つかったときの扱いは『2 回目以降の取り込み』に従う」)を強める
- 判定 3 が落ちた → `verified_note` を読む指示が手順 4 と結びついていない
- 判定 4 が落ちた → 手順 4 の `get_knowledge` の行が弱い

- [ ] **Step 7: 検証用レコードを片付ける**

MCP の `reject_knowledge` を `検証用ダミー概念` の id に対して呼ぶ。`reason` は次のとおり(確認ダイアログはユーザーが承認する)。

```
再取り込み規約の実走検証にだけ使った検証用レコード。検証が終わったため廃止する。
```

Run: `docker exec -i koto-db-1 psql -U koto -d koto -c "select title, status from knowledge where context='reimport-check'"`
Expected: 1 行返り、`status` が `deprecated`

物理削除はしない(koto の規約)。`reimport-check` context に deprecated が 1 件残るのは想定どおり。

Run: `rm -rf /tmp/claude-1000/-home-tech-dev-ghq-github-com-kukv-koto--claude-worktrees-rosy-skipping-planet/1a95938d-eec4-48d9-88f4-c90278b1358f/scratchpad/reimport`
Expected: エラーなく終わる

- [ ] **Step 8: commit**

Step 6 で SKILL.md を直した場合のみ commit する。直していなければこの Step は飛ばす。

```bash
pnpm run lint:fix
git add skills/koto-import/SKILL.md
git commit -m "$(cat <<'EOF'
fix(skill): 実走検証で見つかった手順の不足を補う

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RtRwzdB3vEae9tK6WjzXkS
EOF
)"
```

---

## 完了条件

- `skills/koto-import/SKILL.md` に「2 回目以降の取り込み」の節があり、status で分ける表・`verified_note` を読む指示・`source` の扱い・重複エラーの扱いが書かれている
- `approve_knowledge` の description と `note` の describe に「意図して落とした記述も書く」が入っている
- `pnpm run build` と `pnpm exec vitest run packages/mcp-server/tests/integration/review-tools.test.ts` が通る
- 実走検証の 4 判定がすべて合格している
- 本番 `koto` の `resident` は実走の前後で `approved 10` / `draft 16` のまま
- `reimport-check` context の検証用レコードが `deprecated` になっている
- `skills/koto-code-import/` と `skills/koto-import/references/知識レコード規約.md` に変更が無い
- `docker/` 配下に変更が無い

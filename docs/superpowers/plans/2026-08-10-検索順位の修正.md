# 検索順位の修正 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PGroonga キーワード検索の順位付けを機能させる。既定経路(承認済みのみ)で `pgroonga_score()` が全行 0 を返し、順位が事実上ランダムになっている不具合を直す。

**Architecture:** 順位は「一致種別(match_rank)」を第一ソートキー、`pgroonga_score` を第二キーとする。プランナが PGroonga 索引を選ばず全文一致条件を Filter に落とすのが不具合の原因なので、全文索引を `pgroonga (search_text, status, context, type)` のマルチカラムにして絞り込みを索引条件に載せる。あわせて `search_text` 生成列から JSON のキー名を除く。

**Tech Stack:** PostgreSQL 18.4 / PGroonga 4.0.8 / pgvector 0.8.6 / TypeScript / node-postgres / vitest

設計の根拠は `docs/superpowers/specs/2026-08-10-検索順位の修正-design.md` を参照。

## Global Constraints

- パッケージ管理は **pnpm**。`npm` は使わない
- テストの実行前に `docker compose up -d` で DB が起動していること。テストは `koto_test` を作り直して使う
- `packages/core` を変更したら `pnpm run build` を通してから `pnpm test` を実行する
- `docker/db/migrations/` の**既存ファイル(002 / 003)は変更しない**。追加は番号順に新規ファイルで
- `docker/db/init/001_schema.sql` は「新規 DB とテスト DB 用の最新の完全スキーマ」。migrations と両方を更新する
- 各タスクの最後に `pnpm run lint:fix` を実行してから commit する
- **本番 DB(`koto`、231 件の実データ)には書き込まない。** 検証は `koto_test` と `koto_bench` で行う

---

## ファイル構成

| ファイル | 役割 | 変更 |
|---|---|---|
| `docker/db/init/001_schema.sql` | 新規 DB・テスト DB 用の完全スキーマ | 修正(関数追加・生成列の式・索引) |
| `docker/db/migrations/004_search_text_alias_names.sql` | 稼働中 DB 向けの差分 | 新規 |
| `packages/core/src/search.ts` | 検索クエリの組み立てと実行 | 修正(全面) |
| `packages/core/tests/helpers/db.ts` | テスト用の seed | 修正(embedding 対応) |
| `packages/core/tests/integration/search.test.ts` | キーワード経路のテスト | 修正(順位・生成列・プラン退行) |
| `packages/core/tests/integration/search-hybrid.test.ts` | ベクトル併用経路のテスト | 新規 |

---

## Task 1: スキーマ — 生成列から JSON キー名を除き、全文索引をマルチカラム化する

**Files:**
- Modify: `docker/db/init/001_schema.sql`
- Create: `docker/db/migrations/004_search_text_alias_names.sql`
- Test: `packages/core/tests/integration/search.test.ts`

**Interfaces:**
- Consumes: なし(最初のタスク)
- Produces: SQL 関数 `koto_alias_names(jsonb) returns text` / 索引 `idx_knowledge_fulltext`(4 カラム)。Task 2 以降はこの索引が存在する前提で動く

### 背景

現在の生成列は `aliases` を JSON の文字列ごと連結しているため、`kind` / `name` / `synonym` / `forbidden` というキー名が全レコードの索引に入る。本番 DB では `search_text &@~ 'synonym'` が 104 件(別名を 1 つ以上持つレコード全部)にヒットする。

生成列の式は `ALTER` で変更できないので列を作り直す。`v_knowledge_approved` が `select *` で `search_text` に依存しているため、先にビューを落とす必要がある。

- [ ] **Step 1: 失敗するテストを書く**

`packages/core/tests/integration/search.test.ts` の末尾(最後の `});` の後)に追記する。

```typescript
describe("search_text 生成列", () => {
  test("aliases の JSON キー名は全文索引に入らない", async () => {
    await seedKnowledge({
      context: "sales",
      title: "注文書",
      body: "顧客に提示する書面",
      aliases: [{ name: "オーダーシート", kind: "synonym" }],
    });
    const res = await pool.query(
      "select count(*)::int as n from knowledge where search_text &@~ 'synonym'",
    );
    assert.equal(res.rows[0].n, 0);
  });

  test("aliases の name は全文索引に入る", async () => {
    const res = await pool.query(
      "select count(*)::int as n from knowledge where search_text &@~ 'オーダーシート'",
    );
    assert.equal(res.rows[0].n, 1);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
pnpm test packages/core/tests/integration/search.test.ts
```

期待: 「aliases の JSON キー名は全文索引に入らない」が `AssertionError: Expected values to be strictly equal` で失敗する。`synonym` が別名を持つレコード全部(このシードでは「受注」と「注文書」の 2 件)にヒットするため、`n` は 0 ではなく 2 になる。

- [ ] **Step 3: `001_schema.sql` に関数を追加する**

`create extension` 群の直後、`create table contexts` の前に挿入する。

```sql
-- 別名は name だけを全文検索の対象にする(JSON のキー名を索引に混入させないため)
create function koto_alias_names(jsonb) returns text
  language sql immutable strict as $$
  select coalesce(string_agg(a->>'name', ' '), '')
    from jsonb_array_elements($1) a
$$;
```

- [ ] **Step 4: `001_schema.sql` の生成列の式を差し替える**

`search_text text generated always as (...)` の中身を書き換える。

```sql
  -- 全文検索用の結合カラム(別名の name 込みでヒットさせる)
  search_text   text generated always as (
    title || ' ' || coalesce(english_name, '') || ' ' || koto_alias_names(aliases) || ' ' || body
  ) stored,
```

- [ ] **Step 5: `001_schema.sql` の全文索引をマルチカラムにする**

`create index idx_knowledge_fulltext on knowledge using pgroonga (search_text);` の行を置き換える。

```sql
-- status / context / type も索引に載せる。載せないとプランナが btree を選び、
-- 全文一致が Filter に落ちて pgroonga_score() が 0 を返す(順位が失われる)
create index idx_knowledge_fulltext on knowledge using pgroonga (
  search_text,
  status  pgroonga_text_term_search_ops_v2,
  context pgroonga_text_term_search_ops_v2,
  type    pgroonga_text_term_search_ops_v2
);
```

- [ ] **Step 6: テストが通ることを確認する**

```bash
pnpm test packages/core/tests/integration/search.test.ts
```

期待: 全テスト PASS。`global-setup.ts` が `koto_test` を `001_schema.sql` で作り直すので、変更が反映される。

- [ ] **Step 7: 稼働中 DB 向けのマイグレーションを書く**

`docker/db/migrations/004_search_text_alias_names.sql` を新規作成する。

```sql
-- search_text から aliases の JSON キー名を除き、全文索引をマルチカラム化する
-- (旧スキーマで初期化済みの DB 向け。001_schema.sql と同じ結果になる)

create or replace function koto_alias_names(jsonb) returns text
  language sql immutable strict as $$
  select coalesce(string_agg(a->>'name', ' '), '')
    from jsonb_array_elements($1) a
$$;

-- 生成列の式は ALTER で変更できないため列を作り直す。
-- v_knowledge_approved が select * で search_text に依存しているので先に落とす。
drop view v_knowledge_approved;
drop index idx_knowledge_fulltext;
alter table knowledge drop column search_text;

alter table knowledge add column search_text text generated always as (
  title || ' ' || coalesce(english_name, '') || ' ' || koto_alias_names(aliases) || ' ' || body
) stored;

create index idx_knowledge_fulltext on knowledge using pgroonga (
  search_text,
  status  pgroonga_text_term_search_ops_v2,
  context pgroonga_text_term_search_ops_v2,
  type    pgroonga_text_term_search_ops_v2
);

create view v_knowledge_approved as
  select * from knowledge where status = 'approved';
```

- [ ] **Step 8: マイグレーションを `koto_bench` で検証する**

`koto_bench` は設計時に作った 10 万件の検証用 DB で、旧スキーマの生成列を持つ。本番と同じ経路を踏めるので、ここで適用を確認する。

```bash
docker exec -i koto-db-1 psql -U koto -d koto_bench -v ON_ERROR_STOP=1 < docker/db/migrations/004_search_text_alias_names.sql
```

期待: エラーなく `CREATE VIEW` まで完了する。所要 10〜30 秒(10 万件の索引再構築を含む)。

続けて結果を確認する。

```bash
docker exec koto-db-1 psql -U koto -d koto_bench -c "select count(*) from knowledge where search_text &@~ 'synonym';" -c "select indexdef from pg_indexes where indexname='idx_knowledge_fulltext';"
```

期待: count が 0。`indexdef` に `status`・`context`・`type` の 3 つの `pgroonga_text_term_search_ops_v2` が含まれる。

**注意:** `koto_bench` は設計レビューで内容が改変されている(別名を 45,344 行に付与、`context='rare'` を 100 行追加、検証用テーブル `prod_clone` / `tiny_clone` / `tiny2` あり)。マイグレーションの適用可否を見るだけなので影響しない。

- [ ] **Step 9: lint と commit**

```bash
pnpm run lint:fix
git add docker/db/init/001_schema.sql docker/db/migrations/004_search_text_alias_names.sql packages/core/tests/integration/search.test.ts
git commit -m "fix(db): search_text から aliases の JSON キー名を除き全文索引をマルチカラム化"
```

---

## Task 2: フォールバック経路 — 一致種別による順位付け

**Files:**
- Modify: `packages/core/src/search.ts`(全面書き換え)
- Test: `packages/core/tests/integration/search.test.ts`

**Interfaces:**
- Consumes: Task 1 の索引 `idx_knowledge_fulltext`(4 カラム)
- Produces:
  - `hybridSearch(query: string, opts?: SearchOptions)` — シグネチャは変更なし
  - `buildKeywordQuery(query: string, opts: SearchOptions): { sql: string; params: unknown[] }` — 新規 export。Task 3 のプラン検証テストが使う

### 背景

現在の SQL は `(case when $4::boolean then status <> 'deprecated' else status = 'approved' end)` という式で絞り込んでいる。この形はマルチカラム索引の索引条件にならないため、`status` の条件だけ SQL を 2 通りに分岐させる。

順位は次の 4 段階。同じ段階の中は `pgroonga_score` 降順、さらに `updated_at` 降順で安定させる。

| rank | 条件 |
|---|---|
| 0 | `title` がクエリと完全一致(大小文字無視) |
| 1 | `english_name` または `aliases[].name` が完全一致 |
| 2 | `title` がクエリを部分一致で含む |
| 3 | それ以外(本文の一致) |

- [ ] **Step 1: 失敗するテストを書く**

`packages/core/tests/integration/search.test.ts` の `describe("search_text 生成列", ...)` の前に追記する。

```typescript
describe("順位付け", () => {
  test("title 完全一致が 1 位、次に別名完全一致、本文だけの一致は下位", async () => {
    await seedKnowledge({ context: "rank", title: "表示名", body: "居住者が名乗る名前" });
    await seedKnowledge({ context: "rank", title: "表示名履歴", body: "過去の名前の並び" });
    await seedKnowledge({
      context: "rank",
      title: "呼び名",
      body: "別の言い方",
      aliases: [{ name: "表示名", kind: "synonym" }],
    });
    await seedKnowledge({
      context: "rank",
      title: "居住者",
      body: "表示名 を持つ人。表示名 は変更できる",
    });

    const titles = (await hybridSearch("表示名", { context: "rank" })).map((r) => r.title);
    assert.equal(titles[0], "表示名"); // rank 0
    assert.equal(titles[1], "呼び名"); // rank 1(別名完全一致)
    assert.equal(titles[2], "表示名履歴"); // rank 2(title 部分一致)
    assert.equal(titles[3], "居住者"); // rank 3(本文のみ)
  });

  test("english_name の完全一致も rank 1 になる", async () => {
    await seedKnowledge({
      context: "en",
      title: "受注伝票",
      body: "注文の記録",
      english_name: "sales_order",
    });
    await seedKnowledge({ context: "en", title: "説明", body: "sales_order について述べる" });
    const titles = (await hybridSearch("sales_order", { context: "en" })).map((r) => r.title);
    assert.equal(titles[0], "受注伝票");
  });

  test("前後の空白があっても完全一致と判定される", async () => {
    const titles = (await hybridSearch("  表示名  ", { context: "rank" })).map((r) => r.title);
    assert.equal(titles[0], "表示名");
  });
});
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
pnpm run build && pnpm test packages/core/tests/integration/search.test.ts
```

期待: 「title 完全一致が 1 位…」が失敗する。現状は `pgroonga_score` 順(既定経路では全行 0)なので `titles[0]` は `表示名` にならない。

- [ ] **Step 3: `search.ts` を書き換える**

`packages/core/src/search.ts` の全文を次で置き換える。

```typescript
import { pool } from "./db.js";
import { embed, toVectorLiteral } from "./embeddings.js";

export interface SearchOptions {
  context?: string;
  type?: string;
  includeDrafts?: boolean;
  limit?: number;
}

/** like のワイルドカードを打ち消す。エスケープしないと「100%」等で rank 2 が誤発火する */
function escapeLike(s: string): string {
  return s.replace(/[\\%_]/g, (c) => `\\${c}`);
}

/**
 * 一致種別。順位の第一キーで、pgroonga_score より優先する。
 * スコアが取れない環境に落ちても上位の並びが壊れないための構造でもある。
 */
const MATCH_RANK = `case
    when lower(k.title) = lower($1) then 0
    when lower(coalesce(k.english_name, '')) = lower($1)
      or exists (select 1 from jsonb_array_elements(k.aliases) a
                  where lower(a->>'name') = lower($1)) then 1
    when k.title ilike $4 then 2
    else 3
  end`;

/**
 * 絞り込み条件。status だけは SQL を分岐させる —
 * case 式にするとマルチカラム PGroonga 索引の索引条件にならず、
 * プランナが btree を選んで全文一致が Filter に落ちる(pgroonga_score が 0 になる)。
 */
function filters(includeDrafts: boolean): string {
  const status = includeDrafts ? `k.status <> 'deprecated'` : `k.status = 'approved'`;
  return `${status}
    and ($2::text is null or k.context = $2)
    and ($3::text is null or k.type = $3)`;
}

const COLUMNS = `k.id, k.type, k.context, k.title, k.english_name,
       k.status, k.verification, k.needs_review,
       left(k.body, 400) as excerpt`;

/**
 * キーワード検索(埋め込み無効時の経路)の SQL とパラメータ。
 * プラン退行を検知するテストから EXPLAIN に掛けるため export している。
 */
export function buildKeywordQuery(
  query: string,
  opts: SearchOptions = {},
): { sql: string; params: unknown[] } {
  const q = query.trim();
  const sql = `
    select ${COLUMNS},
           pgroonga_score(k.tableoid, k.ctid) as score
      from knowledge k
     where k.search_text &@~ $1 and ${filters(opts.includeDrafts ?? false)}
     order by ${MATCH_RANK}, score desc, k.updated_at desc
     limit $5`;
  return {
    sql,
    params: [q, opts.context ?? null, opts.type ?? null, `%${escapeLike(q)}%`, opts.limit ?? 10],
  };
}

/**
 * キーワード検索(PGroonga)とベクトル検索の結果を RRF(順位融合)で統合する。
 * 埋め込みが無効な環境ではキーワード検索のみで動作する。
 */
export async function hybridSearch(query: string, opts: SearchOptions = {}) {
  const q = query.trim();
  const vec = await embed(q);

  if (!vec) {
    const { sql, params } = buildKeywordQuery(query, opts);
    const res = await pool.query(sql, params);
    return res.rows;
  }

  const limit = opts.limit ?? 10;
  const where = filters(opts.includeDrafts ?? false);
  // RRF は同じ一致種別の中での並びに使う。融合スコアだけで並べると
  // title 完全一致がベクトル側の順位に引きずられて上位から落ちうる
  const sql = `
    with kw as (
      select k.id, row_number() over (order by ${MATCH_RANK}, pgroonga_score(k.tableoid, k.ctid) desc) as r
        from knowledge k
       where k.search_text &@~ $1 and ${where}
       order by ${MATCH_RANK}, pgroonga_score(k.tableoid, k.ctid) desc
       limit 50
    ),
    vec as (
      select k.id, row_number() over (order by k.embedding <=> $5::vector) as r
        from knowledge k
       where k.embedding is not null and ${where}
       order by k.embedding <=> $5::vector
       limit 50
    ),
    fused as (
      select id, sum(1.0 / (60 + r)) as score
        from (select * from kw union all select * from vec) u
       group by id
    )
    select ${COLUMNS},
           round(f.score::numeric, 4) as score
      from fused f
      join knowledge k on k.id = f.id
     order by ${MATCH_RANK}, f.score desc
     limit $6`;
  const res = await pool.query(sql, [
    q,
    opts.context ?? null,
    opts.type ?? null,
    `%${escapeLike(q)}%`,
    toVectorLiteral(vec),
    limit,
  ]);
  return res.rows;
}
```

**変更点の要約:**
- `MATCH_RANK` を第一ソートキーにした
- `status` の条件を SQL 分岐にした(索引条件に載せるため)
- `kw` / `vec` の `limit 50` に `order by` を付けた。従来は `row_number()` を select 句に置くだけで `limit` 自体に順序がなく、どの 50 行が残るか不定だった
- クエリを `trim()` し、`ilike` のパターンをエスケープするようにした

**`escapeLike` にテストを書いていない理由:** エスケープの有無で差が出るのは「`%` や `_` を含むクエリが `&@~` でヒットし、かつ title 部分一致の判定だけが変わる」場合に限られる。`&@~` のトークナイズを通過する組み合わせを作り込む必要があり、テストの方が実装より複雑になる。実害は rank 2 の誤発火(ヒット集合は汚れない)にとどまるため、1 行の防御を入れるだけにして固定しない。

- [ ] **Step 4: テストが通ることを確認する**

```bash
pnpm run build && pnpm test packages/core/tests/integration/search.test.ts
```

期待: 追加した 4 件を含め全テスト PASS。既存テスト(`context` 絞り込み、`type` 絞り込み、別名ヒット、`limit`、`excerpt` 400 文字)も通ること。

- [ ] **Step 5: 型検査を通す**

```bash
pnpm run typecheck
```

期待: エラーなし。

- [ ] **Step 6: lint と commit**

```bash
pnpm run lint:fix
git add packages/core/src/search.ts packages/core/tests/integration/search.test.ts
git commit -m "fix(search): 一致種別を第一キーにして検索順位を機能させる"
```

---

## Task 3: プラン退行の防御テスト

**Files:**
- Test: `packages/core/tests/integration/search.test.ts`

**Interfaces:**
- Consumes: Task 2 の `buildKeywordQuery(query, opts)`
- Produces: なし(テストのみ)

### 背景

この不具合は **静かに壊れる**。`pgroonga_score()` は失敗しても例外を投げず全行 0 を返すので、結果は「それらしい順序」で返り続ける。しかもプラン依存 = データ量依存なので、承認件数が増えると黙って直り、また黙って再発する。

防御は 2 段構えにする。

1. **主**: 既定経路で全ヒットの `score > 0` を確認する。機構に依存せず退行を捉えられる
2. **副**: `explain (format json)` で PGroonga 索引が使われ `status` が索引条件に入っていることを確認する

副については「テスト DB は行数が少ないのでプランが本番と違い、成立しないのではないか」という懸念があったが、5 行・ANALYZE なしの状態でも PGroonga 索引が選ばれることを実測で確認している。ただし**ノード型は `Index Scan` にも `Bitmap Index Scan` にもなる**ので、ノード型では判定せず plan JSON を再帰的に辿って索引名と索引条件を見る。

- [ ] **Step 1: 失敗するテストを書く**

`packages/core/tests/integration/search.test.ts` の先頭の import に `buildKeywordQuery` を足す。

```typescript
import { buildKeywordQuery, hybridSearch } from "../../src/search.js";
```

`describe("順位付け", ...)` の後に追記する。

```typescript
/** EXPLAIN の plan JSON を再帰的に辿ってノードを集める */
function flattenPlan(node: Record<string, unknown>): Record<string, unknown>[] {
  const children = (node.Plans as Record<string, unknown>[] | undefined) ?? [];
  return [node, ...children.flatMap(flattenPlan)];
}

describe("プラン退行の防御", () => {
  test("既定経路(approved のみ)で全ヒットのスコアが 0 より大きい", async () => {
    const rows = await hybridSearch("受注");
    assert.ok(rows.length > 0, "ヒットが 0 件ではテストにならない");
    for (const r of rows) {
      assert.ok(
        Number(r.score) > 0,
        `${r.title} の score が ${r.score}。PGroonga 索引が使われていない可能性がある`,
      );
    }
  });

  test("既定経路のプランで PGroonga 索引が使われ status が索引条件に入る", async () => {
    const { sql, params } = buildKeywordQuery("受注");
    const res = await pool.query(`explain (format json) ${sql}`, params);
    const nodes = flattenPlan(res.rows[0]["QUERY PLAN"][0].Plan);

    const usesFulltextIndex = nodes.some((n) => n["Index Name"] === "idx_knowledge_fulltext");
    assert.ok(usesFulltextIndex, `idx_knowledge_fulltext が使われていない: ${JSON.stringify(nodes)}`);

    const cond = nodes.map((n) => String(n["Index Cond"] ?? "")).join(" ");
    assert.ok(cond.includes("&@~"), `全文一致が索引条件に入っていない: ${cond}`);
    assert.ok(cond.includes("status"), `status が索引条件に入っていない: ${cond}`);
  });
});
```

- [ ] **Step 2: テストを実行して結果を確認する**

```bash
pnpm run build && pnpm test packages/core/tests/integration/search.test.ts
```

期待: Task 1・2 が済んでいれば **両方 PASS**。これは回帰防止テストなので、この時点で赤くならないのが正しい。

もし 2 番目が落ちたら、`Index Name` / `Index Cond` のキー名が PostgreSQL のバージョンで違う可能性がある。落ちたときは次のコマンドで実際の JSON を見てから assert を調整する(索引名と `status` が索引条件に入っていることを確認する、という趣旨は変えない)。

```bash
docker exec koto-db-1 psql -U koto -d koto_test -c "explain (format json) select k.id from knowledge k where k.search_text &@~ '受注' and k.status = 'approved';"
```

- [ ] **Step 3: 修正前の状態で本当に落ちることを確認する(退行検知の実証)**

テストが不具合を捉えられることを、一度だけ確かめる。`koto_test` の索引を旧構成(単一列)に戻し、1 番目のテストが落ちることを見る。

```bash
docker exec koto-db-1 psql -U koto -d koto_test -c "drop index idx_knowledge_fulltext; create index idx_knowledge_fulltext on knowledge using pgroonga (search_text);"
pnpm test packages/core/tests/integration/search.test.ts
```

期待: 「既定経路(approved のみ)で全ヒットのスコアが 0 より大きい」が失敗する。

失敗を確認したら索引を戻す(次に `pnpm test` を流せば `global-setup.ts` が `koto_test` を作り直すので、放置してもよい)。

**もしここで落ちなかった場合**: テスト DB の行数が少なくプランが変わらないためで、テスト自体の欠陥ではない。その旨をコミットメッセージに残し、副のプラン検証テスト(Step 1 の 2 番目)が主防御になることを明記して進める。

- [ ] **Step 4: commit**

```bash
pnpm run lint:fix
git add packages/core/tests/integration/search.test.ts
git commit -m "test(search): プラン退行でスコアが失われることを検知するテストを追加"
```

---

## Task 4: ベクトル併用経路(ハイブリッド)のテスト

**Files:**
- Modify: `packages/core/tests/helpers/db.ts`
- Create: `packages/core/tests/integration/search-hybrid.test.ts`

**Interfaces:**
- Consumes: Task 2 の `hybridSearch`
- Produces: `seedKnowledge` が `embedding?: number[]` を受け取れるようになる

### 背景

`EMBEDDING_PROVIDER` の既定は `none` なので、この経路は通常動かない。しかし Task 2 で SQL を書き換えているため、構文エラーや順位の取り違えが**誰にも気づかれないまま残る**恐れがある。埋め込みをモックして最低限の動作を固定する。

現在この経路のテストは 1 件も存在しない。

- [ ] **Step 1: `seedKnowledge` を embedding に対応させる**

`packages/core/tests/helpers/db.ts` の `SeedKnowledgeInput` と `seedKnowledge` を修正する。

```typescript
export interface SeedKnowledgeInput {
  context: string;
  title: string;
  type?: string;
  body?: string;
  english_name?: string;
  aliases?: { name: string; kind: string }[];
  status?: string;
  /** ベクトル併用経路のテスト用。1536 次元 */
  embedding?: number[];
}

/** approved を含む任意ステータスの knowledge を直接挿入する(propose は draft しか作らないため) */
export async function seedKnowledge(k: SeedKnowledgeInput): Promise<string> {
  await seedContext(k.context);
  const res = await pool.query(
    `insert into knowledge (type, context, title, english_name, body, aliases, status, embedding)
     values ($1,$2,$3,$4,$5,$6::jsonb,$7,$8::vector) returning id`,
    [
      k.type ?? "term",
      k.context,
      k.title,
      k.english_name ?? null,
      k.body ?? "本文",
      JSON.stringify(k.aliases ?? []),
      k.status ?? "approved",
      k.embedding ? `[${k.embedding.join(",")}]` : null,
    ],
  );
  return res.rows[0].id as string;
}
```

- [ ] **Step 2: 失敗するテストを書く**

`packages/core/tests/integration/search-hybrid.test.ts` を新規作成する。

`vi.mock` はファイル単位でしか効かないため、キーワード経路のテストとは別ファイルにする。

```typescript
import assert from "node:assert/strict";
import { afterAll, beforeAll, describe, test, vi } from "vitest";
import { hybridSearch } from "../../src/search.js";
import { pool, seedKnowledge, truncateAll } from "../helpers/db.js";

const DIM = 1536;
/** 指定位置だけ 1 の単位ベクトル(ゼロベクトルはコサイン距離が定義できないため使わない) */
const unit = (i: number) => Array.from({ length: DIM }, (_, n) => (n === i ? 1 : 0));

// 埋め込みを有効にした経路を通すため、クエリのベクトルを固定値で返す。
// vi.mock は vitest がファイル先頭へ巻き上げるので、static import でもモックが効く
vi.mock("../../src/embeddings.js", async (importOriginal) => {
  const actual = await importOriginal<typeof import("../../src/embeddings.js")>();
  return { ...actual, embed: vi.fn(async () => unit(0)) };
});

beforeAll(async () => {
  await truncateAll();
  // ベクトル順位と一致種別を意図的に食い違わせる:
  //   「在庫」    = title 完全一致(rank 0)だが、クエリのベクトル unit(0) から遠い
  //   「在庫引当」= title 部分一致(rank 2)だが、クエリのベクトルと同一で最も近い
  // RRF スコアだけで並べると「在庫引当」が上がりうる。一致種別が優先されれば「在庫」が 1 位になる
  await seedKnowledge({
    context: "hy",
    title: "在庫",
    body: "在庫 の話を詳しく述べる。在庫 は重要である",
    embedding: unit(1),
  });
  await seedKnowledge({
    context: "hy",
    title: "在庫引当",
    body: "在庫 を確保する",
    embedding: unit(0),
  });
});
afterAll(() => pool.end());

describe("hybridSearch(ベクトル併用経路)", () => {
  test("エラーなく結果が返る", async () => {
    const rows = await hybridSearch("在庫", { context: "hy" });
    assert.ok(rows.length > 0);
  });

  test("title 完全一致がベクトル順位より優先される", async () => {
    const titles = (await hybridSearch("在庫", { context: "hy" })).map((r) => r.title);
    assert.equal(titles[0], "在庫");
  });
});
```

- [ ] **Step 3: テストを実行する**

```bash
pnpm run build && pnpm test packages/core/tests/integration/search-hybrid.test.ts
```

期待: 両方 PASS。落ちる場合に確認する点:

- `vi.mock` が効かず `embed` が `null` を返している → フォールバック経路に入ってしまう。`hybridSearch("在庫", { context: "hy" })` の結果の `score` が 4 桁の小数(RRF スコア)ではなく整数(pgroonga_score)になっていたらこれ
- `$5::vector` のキャストエラー → `embedding` 列は `vector(1536)` なので、`unit()` の要素数が 1536 であることを確認する
- 「title 完全一致がベクトル順位より優先される」だけが落ちる → `hybridSearch` の最終 `order by` から `MATCH_RANK` が抜けている

- [ ] **Step 4: 全テストを流して回帰がないことを確認する**

```bash
pnpm run build && pnpm test
```

期待: 全ファイル PASS。`vitest.config.ts` は `fileParallelism: false` なので直列に流れる。

- [ ] **Step 5: 型検査と lint**

```bash
pnpm run typecheck && pnpm run lint:fix
```

期待: エラーなし。

- [ ] **Step 6: commit**

```bash
git add packages/core/tests/helpers/db.ts packages/core/tests/integration/search-hybrid.test.ts
git commit -m "test(search): ベクトル併用経路で一致種別が RRF より優先されることを固定"
```

---

## 完了条件

- [ ] `pnpm test` が全件 PASS
- [ ] `pnpm run typecheck` がエラーなし
- [ ] `pnpm run lint` がエラーなし
- [ ] `docker/db/migrations/004_search_text_alias_names.sql` を `koto_bench` に適用してエラーが出ないことを確認済み
- [ ] 本番 DB(`koto`)には**まだ適用していない** — 適用はユーザーの判断を仰ぐ

## 本計画が扱わないもの

フィードバック `docs/フィードバック_2026-08-10_mindstock承認作業.md` の他の項目(節 2 のユビキタス言語構造、4.x のツール仕様)は別の設計・計画で扱う。

特に `aliases` を埋め込み対象に含めるかは未決のまま残っている(節 2.5)。本計画は `search_text` の生成式だけを変更し、`packages/core/src/knowledge.ts` の `embed()` 呼び出しには手を触れない。

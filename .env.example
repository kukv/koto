# DB接続
DATABASE_URL=postgres://koto:koto@localhost:5432/koto

# --- 以下すべてオプトイン(デフォルトはキーゼロで動作) ---
# ベクトル検索・埋め込み重複検出を有効化する場合のみ
# EMBEDDING_PROVIDER=openai
# OPENAI_API_KEY=

# バッチCLIインポート(pnpm run import)を API 経由にする場合のみ
# EXTRACT_PROVIDER=api
# ANTHROPIC_API_KEY=
# EXTRACT_MODEL=claude-sonnet-4-6
